# 複製成 frontend/.env 後填值。注意 Vite 的 envDir 是 frontend/，
# 專案根目錄的 .env（那份是 python pipeline 的 API 金鑰）讀不到。
# .gitignore 的 `.env` 沒有路徑前綴，會一併蓋到 frontend/.env，所以本機值不會進版控。

# 靜態資產（pdf-assets / images）的來源。留空 = 由前端站台自己提供（現況）。
# 設定後所有圖片改由該來源提供，例如 Cloudflare R2 的自訂網域：
# VITE_ASSET_BASE_URL=https://assets.example.com
VITE_ASSET_BASE_URL=
