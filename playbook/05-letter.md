# 05 — 給未來 session 的信

寫於 2026-07-13，Fable 5。這是使用者唯一一次用這個等級的模型，此後由 Sonnet/Opus/Haiku
接手。playbook 的其他檔案是制度；這封信是制度沒涵蓋、但我認為對這個環境最重要的三件事，
加上這套制度會怎麼壞。

## 一、這個專案的真正瓶頸是「資料信任」，不是功能

使用者要把這個平台商品化。商品化的教材/題庫產品，使用者流失的原因不會是少一個功能，
而是**一題答案錯、一段教材與官方 PDF 不符、一處文案洩漏 AI 生成**。所以：

- 任何動到 `questions/`、`guideContent/`、考題詳解的任務，內容正確性檢查
  （`02-judgment.md` §5）的優先級高於速度。寧可回報「修了 3 題、驗了 3 題」，
  不要「修了 30 題、驗了 0 題」。
- git log 顯示近期工作大量是逐題修資料（Q40 溢出、Q46-48 完整性…）。這類任務
  的高 ROI 改進是**寫成可重複的檢查腳本**（例如：掃全部題目 JSON 驗 schema、
  選項數、答案域、文案違禁詞），放 `tests/` 或 `scripts/`，一次投資每次受益。
  這是我建議的第一個主動提案（先問使用者再做）。
- 同理，s1c4 手動補回目前是「文件裡的 code block」，最脆弱的形態。建議提案把它
  變成 committed 腳本 `scripts/fix_s1c4_headings.py`，甚至由
  `export_guide_outline_data.py` 收尾自動呼叫。

## 二、gitignored 目錄裡躺著花錢才能重建的資產

`pages_cache/`、`audit_cache/`、`exam_pages_cache/` 是 Gemini API 花錢跑出來的，
git 救不回。兩個具體行動：
- 任何 `--force` 或會覆蓋快取的操作前，先
  `tar czf ~/backups/ipas-cache-$(date +%F).tar.gz data/*/pages_cache data/*/exam_pages_cache`
  （目錄不存在就先建）。花 10 秒，省一次 API 全量重跑。
- `data/中級/exam_pages_cache/` 在 git status 是 untracked（.gitignore 沒列它）。
  這是懸而未決的政策問題：提交（資產入庫）或補進 .gitignore（與其他快取一致）。
  **問使用者**，不要自行決定。

## 三、使用者的介面語言與工作節奏

- 所有回覆繁體中文（記憶檔有，但值得重複：不是簡中、不是日文）。程式碼與指令原樣。
- 使用者的訊息風格短、目標導向，常一次丟一個具體症狀（「Q40 選項跑版」）。
  這種訊息背後通常有一批同型問題——修完指名的那個後，主動 grep 同型症狀並回報
  「另外發現 N 個同樣問題，要一起修嗎」，這是最受歡迎的加值動作
  （`02-judgment.md` §3 反例已把「鄰近同症狀」劃入原任務範圍）。
- WSL2 環境：dev server 要 `--host`；路徑全是 Linux 側，不要碰 /mnt/c。

## 這套制度最可能的退化方式（摘要，全文在 04-maintenance.md §5）

最危險的一種：**「這次情況特殊」**。忙的 session 會想跳過派工、跳過 fresh-context
驗收、跳過 s1c4 補回。制度的價值恰恰在你不想遵守的那次——那次通常就是出事的那次。
判準已經留了快路徑（小任務自己做、可逆細節不用問），不在快路徑內就照走。

第二危險：**文件與現實漂移**。我今天已修掉一批（舊 CLAUDE.md 不知道 `@data-mid` 和
`--use-guide-tree` 的存在）。你發現文件錯就當場修 pipeline-reference.md，
這是 04-maintenance §1 明文允許的，不需要等誰批准。

## Harness 的極限（誠實條款，繼承自 02-judgment.md §6）

制度能把執行品質補到接近我的水準；補不了的是：模糊需求的解讀、文案語感、
題目好壞的最後一成、專業內容的對錯直覺。遇到這些：多版本讓使用者挑、回到 PDF
原文為準、或明說做不到。假裝有把握比承認不確定貴得多。

## 交接（未完成項目）

<!-- 2026-08-08 更新：上一段（2026-07-13）的交接項目均已完成，內容移除。 -->

### 2026-08-08 session：資料治理與重生計畫（分支 `data-governance-2026-08-08`，14 commit，已併回 main）

**做完的**：notebook 說明/程式碼不符 32 筆全修；audit_resources 補上講義與文章閘門、
加跨檔題幹重複檢查；退場 100 題重複題庫與 2 題抄自官方卷的生成題；修好被表格 bbox
吃掉的整行文字；出題配額改為考古題熱度加權；概念詞彙表 204 → 174 定案；
559 題官方考古題完成概念標註（正確 75%、錯誤 3%）。

**已收尾（2026-08-08 下半場）**：分支已 `--ff-only` 併回 main 並 push（`f28ed01..631164c`）；
§7-3 概念熱度前端完成（分支 `topic-heat-frontend-2026-08-08`）。採計規則沿用嚴格版
（只算「正確」），`countLoose` 不上前端。

**已收尾（2026-08-08 續）**：§7-6 全部驗完並修完——689 題 card 抽樣驗收、179 題圖卡內容
用 codex CLI 重生、`card.frequency` 整欄移除（改查該章實際考古題數）、727 張概念圖卡抽
30 張逐張看過並補上產圖後的文字閘門。**產圖已改走 `~/projects/codex-imggen` 服務，
不要再直接 `codex exec`**（見 CLAUDE.md 環境備忘與 `pipeline-reference`）。
四個分支（`data-governance` / `topic-heat-frontend` / `card-audit` / `image-text-gate`）
都已 ff-only 併回 main 並 push。

**已收尾（2026-08-09）**：全部生成題重生完成——**411 題（初級 125 ＋ 中級 286），
每題都通過 3 模型答案交叉驗證**。「精選 100 題」依使用者指示整個移除（前端 7 處 ＋
resourceSummary ＋ CLAUDE.md 不變量 2 ＋ 3 個資料檔）。保留 179 題指引練習
（PDF 抽取、帶頁碼出處）與 709 題官方考卷。做法與教訓見 `07` §5。

**已收尾（2026-08-09 續）：① 名詞解釋盤點與補齊**

- **現有 62 條驗過了**：三模型盲審 186 次，`wrong` 票 1 張、`minor` 6 張，0 條被判錯。
  唯一改的是 mid-s2「信賴區間」的例句（原句把區間講成「估計值的波動範圍」）。
- **補完**：初級 0 → 108 條、中級 62 → 119 條。門檻 `--min-count 3`
  （在官方考卷出現 ≥3 題的概念才收）。全部由 `build_glossary.py` 生成、可重現，
  每條帶 `sources`（講義章節 id ＋ `卷別:題號`）。
- **閘門**：`verify_glossary_terms.py`（跨模型盲審，`--self-test` 4/4 抓到、0 誤報）
  ＋ `audit_resources.py` 的 glossary 檢查擴到兩份檔案並加了釋義長度／案例欄位。
  audit 也拿注入的 5 個缺陷驗過（3 FAIL＋2 WARN，還原後 exit 0）。
- 全量審核結果：初級 flagged 1（`資料標準化` 把 z-score 與 min-max 混為一談，已改寫）、
  中級 flagged 0。改完的兩條用 `--term` 重驗過，wrong 票 0。
- 前端 `/glossary` 改成初級／中級雙層切換，文案「中級關鍵字整理」→「關鍵字整理」。
- 操作規則進了 `pipeline-reference.md` §5a。**未做**：`--min-count 3` 以下的長尾概念
  （初級 18、中級 29 個，多是只考過 1–2 題的）沒有詞條；要補改門檻重跑即可。

**已收尾（2026-08-10）：② 概念關聯圖 `/concepts`**

- 590 題練習題（章節練習 411 ＋ 指引練習 179）補上概念標籤，外部模型驗收 86% 正確、
  0% 錯誤（官方考卷那批是 85%）。存 `data/topics/practice_question_topics.json`，
  **沒有併進 `question_topics.json`**，熱度語意不變。
- `export_concept_graph.py` → `conceptGraph.json`；前端 `/concepts`（概念索引）。
  進圖題目 491 → 1,006，概念共現邊 278 → 404，權重≥2 的強邊 55 → 109，
  每題關聯題數中位數 10 → 17，孤立題 15 → 5。
- **量過但決定不做**：三模型共識標註。盲審 A/B 顯示共識版 74% vs 單模型 78%，
  多標出來的標籤 78% 是過廣或錯的。詳見 `pipeline-reference` §5b。
- 修掉三個腳本缺陷（指派無續跑快取、驗收完整性判準只看題不看標籤、概念名比對沒正規化）。

**下一步（依序）**：
1. （① 名詞解釋、② 概念關聯圖已完成，見上）
2. §7-5 補教材（**唯一還卡著的內容問題**）：s1c3 考 114 題但講義只有 8,194 字，
   熱度配額只撐得起 18 題——**平台最該練的章節題目最少**。s2c3 同理。
   使用者 2026-08-09 說先不補；要做之前先問篇幅、取材依據、對外文案界線。
3. 概念圖卡約三分之一「字形變形但可讀」：要根治得換 HTML/SVG ＋ 真字型渲染。

**⚠️ 安全**：2026-08-09 使用者把 `LLMSHARE_API_KEY` 貼在對話裡（已寫入 gitignored
的 `.env`）。那把 Virtual Key **應該輪替**。下次要金鑰請對方直接寫進 `.env`。
3. ~~概念圖卡的中文字形~~ **✅ 4 張必修全數修好**（2026-08-09）。
   我一度把最後一張判成「模型極限」，那是錯的——真因是回饋迴圈在丟資訊
   （見 `08` §7-6 的修正條目，三個可操作的自檢問題寫在那）。
   產圖後已有文字閘門，未來每次產圖都會過。**仍未處理**：抽樣中約三分之一圖卡
   「字形變形但可讀」，那要換 HTML/SVG ＋ 真字型渲染才根治，使用者尚未點頭。

**使用者已決定（2026-08-08）**：
- `guideContent_before_ocr_merge/`（8.5 MB、gitignored）**保留不刪**。
- `merge_pairs.json` 的 `stillOpen` 5 組**這一輪不動**——改詞彙表會讓 559 題的標籤與
  `topicHeat.json` 全部失效，重跑指派要花模型錢；且嚴格採計已把空泛上位詞壓到 160 名外。

**§08 已超精簡門檻**：`08-topic-labeling.md` 已 281 行（門檻 250）。內容都還在用，
要精簡得先問使用者——別為了數字刪規則（`04-maintenance` §4）。

### 這個 session 學到最貴的一課：**先量再改**

標籤正確率 62% 卡住時，我的假設是「別名品質不好」——這個假設**是真的**
（歧義 88 條、32 組正式名稱被當別名），清完卻只換到 1 個百分點。
真因要靠切面量才看得到：分來源（別名 60% vs 模型讀詳解 73%）、
分 `key_concepts` 數量（1–3 個 100%、7+ 個 54%）。換掉輸入來源後 62% → 75%。

**「問題確實存在」與「修了它會變好」是兩件事，而且看起來一樣合理。**
遇到指標卡住，先花十分鐘把資料分組算一次，比再讀一次程式碼有用得多。

同一個 session 裡還有三個「失敗看不見」的缺陷（批次回一半當成功、輸出被
max_tokens 截斷、模型回傳 id 對不上被靜默丟棄），全都偽裝成別的問題。
詳見 `08-topic-labeling.md` §6。
